import React from 'react';

import styles from './Footer.module.css';

const Footer = () => (
    <h1 className={styles.heading}>
        <span className={styles.light}>Made with</span> <span className={styles.love}>Love</span>

    </h1>
);

export default Footer;